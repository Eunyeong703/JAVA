추상화
- 클래스를 설계도라고 이야기함.
- 추상화 클래스 : 미완성 설계도
- 하나 이상의 추상 메서드를 가지고 있는 클래스를 추상 클래스라고 한다.
- 추상 클래스는 객체의 생성이 불가능하다.
- 추상 클래스를 사용하는 이유는 다른 클래스를 작성하는데 도움을 주기 위해서 만든 클래스이다.

추상 메서드
- 선언부만 있고 내용이 없는 메서드를 의미
- 꼭 필요하지만, 자식 클래스에서 다르게 구현하도록 만들 때 사용한다.
- 추상 클래스를 상속받는 클래스는 반드시 이 추상 메서드를 구현해야 한다.
- 키워드는 abstract이다.